kubectl create secret generic gcp-sa --from-file=key1.json=sa-monitoring-test.json -n monitoring

---

kubectl create secret generic gcp-sa \
  --from-file=key.json=service-account-key.json \
  -n [namespace]
